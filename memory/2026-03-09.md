# 2026-03-09 开发记录

## 完成
- 确定产品需求：AI智能任务管理系统（CoWorker）
- 完成技术选型：Next.js + Tailwind + Drizzle + Qwen/DeepSeek
- 完成方案设计和评审
- 创建项目文档结构（docs/ + memory/）
- Phase 1: Next.js项目初始化 + SQLite数据库 + Drizzle ORM + migration
- Phase 2: 任务CRUD API（GET/POST/PUT/DELETE）+ 基础UI（TaskCard、底部导航、首页今日视图、任务列表页）
- Phase 3: AI任务提取（Qwen/DeepSeek双供应商，自然语言→结构化JSON）
- Phase 4: 语音输入（浏览器PCM录音 + Qwen3-ASR WebSocket转写 + 自动送入AI提取）
- Phase 5: 报告生成（日报/周报/项目报告，AI自动汇总）
- Phase 6: 应用内提醒（逾期/今日到期任务高亮提醒）
- 全部代码编译通过（next build成功）

## 问题/决策
- AI供应商：同时支持Qwen3.5-Plus和DeepSeek-V3.2，通过AI_PROVIDER环境变量切换
- ASR：使用阿里云Qwen3-ASR-Flash-Realtime，服务端WebSocket代理
- V1只做自用，不做跨部门协作通知
- V1只做应用内提醒，不做推送通知
- 数据库开发用SQLite，生产用Turso

## 已部署
- Git仓库已推送到 https://github.com/irisfeng/coworker.git
- 数据库已迁移到Turso（libsql）
- DashScope已切换为新加坡国际端点（dashscope-intl.aliyuncs.com）
- UI已重设计为Anthropic暖色风格
- 已部署到Vercel
- Cloudflare域名：ddai.monster（DNS配置中）

## 待完成
- 确认Cloudflare DNS配置正确（代理模式需关闭）
- 验证线上完整流程（任务创建/AI提取/语音/报告）
- 本地测试配置API Key
## 2026-03-09 追加记录
- 排查线上 Vercel 和本地的语音转写、AI 提取、任务落库链路，确认问题集中在 `/api/asr` 和 `/api/ai/extract`
- 调整 `src/app/api/asr/route.ts`，为 ASR 增加请求级日志，并把空转录和连接提前关闭改为显式失败
- 调整 `src/components/VoiceButton.tsx`，前端对 `/api/asr` 非 200 和空转录结果显式报错
- 调整 `src/app/page.tsx`，AI 提取返回空 `tasks` 时不再默认落库，避免闲聊被记录为待办
- 调整 `src/app/api/ai/extract/route.ts`，补充日志和兜底规则，只对明显像任务的输入兜底创建任务
- 扩大 Todo 提取范围，将“参加会议、见客户、参观学习、培训、汇报、出差、调研、对接”等日程型事项纳入待办
- 已推送 GitHub `main`：`26cfcf9 Fix voice task extraction flow`
- 已推送 GitHub `main`：`7bd013e Expand todo extraction rules`
- 定位 `POST /api/tasks 400` 根因：AI 提取结果中的 `description/collaborator/project_tag` 等字段为 `null`，但任务创建接口原先只接受 `string | undefined`
- 调整 `src/app/api/tasks/route.ts`，任务创建和更新接口兼容 `null` 输入并补充校验失败日志
- 调整 `src/hooks/useTasks.ts`，任务创建失败时在浏览器控制台打印后端错误体，便于本地调试
- 调整 `src/app/page.tsx`，首页从仅展示“今天”扩展为展示“今天、未来 7 天、待安排、更后续安排”，更符合按周安排事项的使用习惯
- 评估日期时间交互优化后，采用“兼容现有库结构”的方案：继续复用 `due_date` 字段，同时支持 `YYYY-MM-DD` 和 `YYYY-MM-DD HH:mm`
- 新增 `src/lib/due.ts`，统一处理截止日期/时间的解析、排序、逾期判断和展示文案
- 调整 `src/components/TaskCard.tsx`，新增原生日期和时间选择器，可在任务卡片上直接调整和清除时间安排
- 调整 `src/app/page.tsx`、`src/app/tasks/page.tsx`、`src/hooks/useReminders.ts`，统一按 `due_date` 的日期部分做首页分组、任务排序和提醒判断
- 调整 `src/app/api/tasks/route.ts`，`today/overdue` 查询改为按日期部分过滤，兼容带时间的 `due_date`
- 调整 `src/app/api/ai/extract/route.ts`，要求模型在识别到明确时间时输出 `YYYY-MM-DD HH:mm`
- 调整 `src/hooks/useTasks.ts`，抽象通用任务更新接口并处理异步刷新，支持任务卡片直接保存日期时间
